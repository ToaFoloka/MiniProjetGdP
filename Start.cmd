REM Des fois il faut rafraichir plusieurs fois le navigateur et lancer manuellement quelques fois. Normalement c'est pas la norme donc pas de soucis :)
docker-compose up -d --force-recreate --no-deps --build
